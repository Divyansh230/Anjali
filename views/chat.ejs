<!DOCTYPE html>
<html>
<head>
  <title>Chat with Anjali (Your Ex) ❤️</title>
  <style>
    /* Romantic Chat Page Styles */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Georgia', 'Times New Roman', serif;
      background: linear-gradient(135deg, #ff9a9e 0%, #fecfef 50%, #fecfef 100%);
      min-height: 100vh;
      padding: 20px;
      position: relative;
      overflow-x: hidden;
    }

    /* Romantic background hearts animation */
    body::before {
      content: '';
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background-image: 
        radial-gradient(circle at 20% 20%, rgba(255, 182, 193, 0.3) 2px, transparent 2px),
        radial-gradient(circle at 80% 80%, rgba(255, 192, 203, 0.2) 1px, transparent 1px),
        radial-gradient(circle at 40% 60%, rgba(255, 105, 180, 0.1) 1px, transparent 1px);
      background-size: 100px 100px, 150px 150px, 200px 200px;
      animation: float 20s ease-in-out infinite;
      pointer-events: none;
      z-index: -1;
    }

    @keyframes float {
      0%, 100% { transform: translateY(0px) rotate(0deg); }
      33% { transform: translateY(-10px) rotate(1deg); }
      66% { transform: translateY(-5px) rotate(-1deg); }
    }

    /* Profile section */
    .profile-section {
      text-align: center;
      margin-bottom: 30px;
      position: relative;
    }

    .profile-container {
      display: inline-block;
      position: relative;
      margin-bottom: 20px;
    }

    .profile-pic {
      width: 150px;
      height: 150px;
      border-radius: 50%;
      border: 5px solid #ff69b4;
      box-shadow: 0 10px 30px rgba(255, 105, 180, 0.4);
      object-fit: cover;
      animation: profileGlow 3s ease-in-out infinite;
      transition: transform 0.3s ease;
    }

    .profile-pic:hover {
      transform: scale(1.05);
    }

    @keyframes profileGlow {
      0%, 100% { box-shadow: 0 10px 30px rgba(255, 105, 180, 0.4); }
      50% { box-shadow: 0 15px 40px rgba(255, 105, 180, 0.8); }
    }

    .profile-container::before {
      content: '';
      position: absolute;
      top: -10px;
      left: -10px;
      right: -10px;
      bottom: -10px;
      border-radius: 50%;
      background: linear-gradient(45deg, #ff69b4, #ff1493, #dc143c, #ff69b4);
      z-index: -1;
      animation: rotate 4s linear infinite;
    }

    @keyframes rotate {
      0% { transform: rotate(0deg); }
      100% { transform: rotate(360deg); }
    }

    /* Header styling */
    h1 {
      font-size: 3rem;
      color: #8b0000;
      text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
      margin: 20px 0 10px;
      font-weight: bold;
      letter-spacing: 2px;
      position: relative;
    }

    h1::after {
      content: '💕';
      position: absolute;
      top: -10px;
      right: -50px;
      font-size: 2rem;
      animation: heartbeat 2s ease-in-out infinite;
    }

    @keyframes heartbeat {
      0%, 100% { transform: scale(1); }
      50% { transform: scale(1.2); }
    }

    .status-text {
      color: #8b0000;
      font-size: 1.2rem;
      font-style: italic;
      margin-bottom: 20px;
      text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.1);
    }

    /* Chat container */
    .chat-container {
      max-width: 800px;
      margin: 0 auto 30px;
      background: rgba(255, 255, 255, 0.9);
      border-radius: 25px;
      padding: 30px;
      box-shadow: 0 15px 35px rgba(0, 0, 0, 0.1);
      backdrop-filter: blur(10px);
      border: 2px solid rgba(255, 182, 193, 0.3);
      min-height: 400px;
      position: relative;
    }

    .chat-container::before {
      content: '';
      position: absolute;
      top: -5px;
      left: -5px;
      right: -5px;
      bottom: -5px;
      background: linear-gradient(45deg, #ff69b4, #ff1493, #dc143c, #ff69b4);
      border-radius: 30px;
      z-index: -1;
      animation: borderGlow 3s linear infinite;
    }

    @keyframes borderGlow {
      0%, 100% { opacity: 0.5; }
      50% { opacity: 1; }
    }

    /* Message styling */
    .user-msg, .ai-msg {
      margin: 20px 0;
      padding: 15px 20px;
      border-radius: 20px;
      font-size: 1.1rem;
      line-height: 1.6;
      position: relative;
    }

    .user-msg {
      background: linear-gradient(135deg, #ff69b4, #ff1493);
      color: white;
      margin-left: 80px;
      text-align: right;
      box-shadow: 0 5px 15px rgba(255, 105, 180, 0.3);
    }

    .user-msg::before {
      content: '💌';
      position: absolute;
      left: -50px;
      top: 50%;
      transform: translateY(-50%);
      font-size: 1.5rem;
    }

    .ai-msg {
      background: linear-gradient(135deg, #ffc0cb, #ffb6c1);
      color: #8b0000;
      margin-right: 80px;
      text-align: left;
      box-shadow: 0 5px 15px rgba(255, 192, 203, 0.3);
    }

    .ai-msg::after {
      content: '🌹';
      position: absolute;
      right: -50px;
      top: 50%;
      transform: translateY(-50%);
      font-size: 1.5rem;
    }

    .user-msg strong, .ai-msg strong {
      font-weight: bold;
      text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.2);
    }

    /* Form styling */
    form {
      max-width: 800px;
      margin: 0 auto;
      display: flex;
      gap: 15px;
      padding: 20px;
      background: rgba(255, 255, 255, 0.8);
      border-radius: 25px;
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
      backdrop-filter: blur(5px);
    }

    input[type="text"] {
      flex: 1;
      padding: 15px 20px;
      border: 2px solid #ffb6c1;
      border-radius: 25px;
      font-size: 1rem;
      outline: none;
      background: rgba(255, 255, 255, 0.9);
      color: #8b0000;
      transition: all 0.3s ease;
    }

    input[type="text"]:focus {
      border-color: #ff69b4;
      box-shadow: 0 0 15px rgba(255, 105, 180, 0.3);
      transform: translateY(-2px);
    }

    input[type="text"]::placeholder {
      color: #cd5c5c;
      font-style: italic;
    }

    button {
      padding: 15px 30px;
      background: linear-gradient(135deg, #ff1493, #dc143c);
      color: white;
      border: none;
      border-radius: 25px;
      font-size: 1rem;
      font-weight: bold;
      cursor: pointer;
      transition: all 0.3s ease;
      box-shadow: 0 5px 15px rgba(220, 20, 60, 0.3);
      position: relative;
      overflow: hidden;
    }

    button::before {
      content: '';
      position: absolute;
      top: 0;
      left: -100%;
      width: 100%;
      height: 100%;
      background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
      transition: left 0.5s;
    }

    button:hover {
      transform: translateY(-3px);
      box-shadow: 0 8px 20px rgba(220, 20, 60, 0.4);
    }

    button:hover::before {
      left: 100%;
    }

    button:active {
      transform: translateY(-1px);
    }

    /* Responsive design */
    @media (max-width: 768px) {
      body {
        padding: 10px;
      }
      
      .profile-pic {
        width: 120px;
        height: 120px;
      }
      
      h1 {
        font-size: 2rem;
      }
      
      h1::after {
        right: -30px;
        font-size: 1.5rem;
      }
      
      .chat-container {
        padding: 20px;
        margin-bottom: 20px;
      }
      
      .user-msg {
        margin-left: 40px;
      }
      
      .ai-msg {
        margin-right: 40px;
      }
      
      .user-msg::before, .ai-msg::after {
        font-size: 1.2rem;
      }
      
      form {
        flex-direction: column;
        gap: 10px;
      }
      
      button {
        padding: 12px 25px;
      }
    }

    /* Extra romantic touches */
    @keyframes sparkle {
      0%, 100% { opacity: 0; }
      50% { opacity: 1; }
    }

    .chat-container::after {
      content: '✨';
      position: absolute;
      top: 20px;
      right: 30px;
      font-size: 1.5rem;
      animation: sparkle 2s ease-in-out infinite;
    }

    /* Scrollbar styling */
    ::-webkit-scrollbar {
      width: 8px;
    }

    ::-webkit-scrollbar-track {
      background: rgba(255, 182, 193, 0.1);
      border-radius: 10px;
    }

    ::-webkit-scrollbar-thumb {
      background: linear-gradient(135deg, #ff69b4, #ff1493);
      border-radius: 10px;
    }

    ::-webkit-scrollbar-thumb:hover {
      background: linear-gradient(135deg, #ff1493, #dc143c);
    }
  </style>
</head>
<body>
  <!-- Profile Section -->
  <div class="profile-section">
    <div class="profile-container">
      <img src="https://images.unsplash.com/photo-1630568667468-fe2a669d8861?q=80&w=2070&auto=format&fit=crop&ixlib=rb-4.1.0&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D" alt="Anjali" class="profile-pic">
    </div>
    <h1>Anjali 😍</h1>
    <p class="status-text">Online • Your Beautiful Love 💖</p>
  </div>

  <!-- Chat Container -->
  <div class="chat-container">
    <div class="user-msg"><strong>You:</strong> <%= userMessage %></div>
    <div class="ai-msg"><strong>Anjali:</strong> <%= aiResponse %></div>
  </div>

  <!-- Message Form -->
  <form action="/chat" method="POST">
    <input type="text" name="message" placeholder="Type your message to Anjali..." required>
    <button type="submit">Send 💕</button>
  </form>
</body>
</html>